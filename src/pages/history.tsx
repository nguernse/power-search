/**
 * @todo Load the history from localStorage.
 * @todo Allow removing history items.
 * @todo Allow clearing all history.
 * @todo Allow toggling saving search history.
 */
export default function SearchHistory() {
  return (
    <div>
      <h1>Search History</h1>
      <p>List search history here...</p>
    </div>
  );
}
